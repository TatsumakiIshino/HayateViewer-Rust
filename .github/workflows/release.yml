name: Build and Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build and Release
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Install dependencies via vcpkg
        run: vcpkg install dav1d:x64-windows pkgconf:x64-windows

      - name: Build release binary
        shell: pwsh
        run: |
          $env:PKG_CONFIG = "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\tools\pkgconf\pkgconf.exe"
          $env:PKG_CONFIG_PATH = "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\lib\pkgconfig"
          cargo build --release

      - name: Prepare release assets
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}"
          $fileName = "HayateViewer-Rust-$version-windows-x64.zip"
          Copy-Item "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows\bin\dav1d.dll" -Destination "target/release/"
          Compress-Archive -Path "target/release/HayateViewer-Rust.exe", "target/release/dav1d.dll", "README.md", "CHANGELOG.md" -DestinationPath $fileName
          echo "ASSET_PATH=$fileName" >> $env:GITHUB_ENV

      - name: Extract release notes
        id: extract_notes
        shell: pwsh
        run: |
          $changelog = Get-Content -Path CHANGELOG.md -Encoding UTF8
          $found = $false
          $notes = ""
          foreach ($line in $changelog) {
              if ($line -match '^## \[') {
                  if ($found) { break }
                  $found = $true
                  continue
              }
              if ($found) {
                  $notes += $line + "`n"
              }
          }
          $notes | Out-File -FilePath release_notes.md -Encoding UTF8
          echo "notes_file=release_notes.md" >> $env:GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ASSET_PATH }}
          body_path: release_notes.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
